@using HealthManagmentSystem.Models.ViewModel
@model AdminUserViewModel
@{
    ViewData["Title"] = "User Management";
    Layout = "LayoutAdmin";
}

<div class="container mt-4">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h2>User Management</h2>
        <a asp-action="AddUser" class="btn btn-primary">
            <i class="fas fa-user-plus"></i> Add New User
        </a>
    </div>

    <!-- Tab Navigation -->
    <ul class="nav nav-tabs" id="userTabs" role="tablist">
        <li class="nav-item">
            <a class="nav-link active" id="admins-tab" data-toggle="tab" href="#admins" role="tab">Admins</a>
        </li>
        <li class="nav-item">
            <a class="nav-link" id="doctors-tab" data-toggle="tab" href="#doctors" role="tab">Doctors</a>
        </li>
        <li class="nav-item">
            <a class="nav-link" id="managers-tab" data-toggle="tab" href="#managers" role="tab">Managers</a>
        </li>
        <li class="nav-item">
            <a class="nav-link" id="patients-tab" data-toggle="tab" href="#patients" role="tab">Patients</a>
        </li>
    </ul>

    <!-- Tab Content -->
    <div class="tab-content p-3 border border-top-0 rounded-bottom" id="userTabsContent">
        <!-- Admins Tab -->
        <div class="tab-pane fade show active" id="admins" role="tabpanel">
            @if (Model.Admins.Any())
            {
                <table class="table table-hover">
                    <thead class="thead-light">
                        <tr>
                            <th>Name</th>
                            <th>Email</th>
                            <th class="text-right">Actions</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var admin in Model.Admins)
                        {
                            <tr>
                                <td>@admin.Name</td>
                                <td>@admin.Email</td>
                                <td class="text-right">
                                    <div class="d-inline-flex">
                                        <a asp-action="EditUser" asp-route-id="@admin.Id" asp-route-role="Admin" class="btn btn-sm btn-outline-secondary mr-1" title="Edit">
                                            <i class="fas fa-edit"></i> Edit
                                        </a>
                                        <form asp-action="DeleteUser" method="post" onsubmit="return confirm('Are you sure you want to delete this admin?');">
                                            <input type="hidden" name="id" value="@admin.Id" />
                                            <input type="hidden" name="role" value="Admin" />
                                            <button type="submit" class="btn btn-sm btn-outline-danger" title="Delete">
                                                <i class="fas fa-trash-alt"></i> Delete
                                            </button>
                                        </form>
                                    </div>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            }
            else
            {
                <div class="alert alert-info">No administrators found</div>
            }
        </div>

        <!-- Doctors Tab -->
        <div class="tab-pane fade" id="doctors" role="tabpanel">
            @if (Model.Doctors.Any())
            {
                <table class="table table-hover">
                    <thead class="thead-light">
                        <tr>
                            <th>ID</th>
                            <th>Name</th>
                            <th>Email</th>
                            <th>Specialization</th>
                            <th class="text-right">Actions</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var doctor in Model.Doctors)
                        {
                            <tr>
                                <td>@doctor.ID</td>
                                <td>@doctor.Name</td>
                                <td>@doctor.Email</td>
                              
                                <td class="text-right">
                                    <div class="d-inline-flex">
                                        <a asp-action="EditUser" asp-route-id="@doctor.ID" asp-route-role="Doctor" class="btn btn-sm btn-outline-secondary mr-1" title="Edit">
                                            <i class="fas fa-edit"></i> Edit
                                        </a>
                                        <form asp-action="DeleteUser" method="post" onsubmit="return confirm('Are you sure you want to delete this doctor?');">
                                            <input type="hidden" name="id" value="@doctor.ID" />
                                            <input type="hidden" name="role" value="Doctor" />
                                            <button type="submit" class="btn btn-sm btn-outline-danger" title="Delete">
                                                <i class="fas fa-trash-alt"></i> Delete
                                            </button>
                                        </form>
                                    </div>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            }
            else
            {
                <div class="alert alert-info">No doctors found</div>
            }
        </div>

        <!-- Managers Tab -->
        <div class="tab-pane fade" id="managers" role="tabpanel">
            @if (Model.Managers.Any())
            {
                <table class="table table-hover">
                    <thead class="thead-light">
                        <tr>
                            <th>ID</th>
                            <th>Name</th>
                            <th>Email</th>
                            <th>Department</th>
                            <th class="text-right">Actions</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var manager in Model.Managers)
                        {
                            <tr>
                                <td>@manager.ID</td>
                                <td>@manager.Name</td>
                                <td>@manager.Email</td>

                                <td class="text-right">
                                    <div class="d-inline-flex">
                                        <a asp-action="EditUser" asp-route-id="@manager.ID" asp-route-role="Manager" class="btn btn-sm btn-outline-secondary mr-1" title="Edit">
                                            <i class="fas fa-edit"></i> Edit
                                        </a>
                                        <form asp-action="DeleteUser" method="post" onsubmit="return confirm('Are you sure you want to delete this manager?');">
                                            <input type="hidden" name="id" value="@manager.ID" />
                                            <input type="hidden" name="role" value="Manager" />
                                            <button type="submit" class="btn btn-sm btn-outline-danger" title="Delete">
                                                <i class="fas fa-trash-alt"></i> Delete
                                            </button>
                                        </form>
                                    </div>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            }
            else
            {
                <div class="alert alert-info">No managers found</div>
            }
        </div>

        <!-- Patients Tab -->
        <div class="tab-pane fade" id="patients" role="tabpanel">
            @if (Model.Patients.Any())
            {
                <table class="table table-hover">
                    <thead class="thead-light">
                        <tr>
                            <th>ID</th>
                            <th>Name</th>
                            <th>Email</th>
                            <th>Date of Birth</th>
                            <th class="text-right">Actions</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var patient in Model.Patients)
                        {
                            <tr>
                                <td>@patient.ID</td>
                                <td>@patient.Name</td>
                                <td>@patient.Email</td>
                               
                                <td class="text-right">
                                    <div class="d-inline-flex">
                                        <a asp-action="EditUser" asp-route-id="@patient.ID" asp-route-role="Patient" class="btn btn-sm btn-outline-secondary mr-1" title="Edit">
                                            <i class="fas fa-edit"></i> Edit
                                        </a>
                                        <form asp-action="DeleteUser" method="post" onsubmit="return confirm('Are you sure you want to delete this patient?');">
                                            <input type="hidden" name="id" value="@patient.ID" />
                                            <input type="hidden" name="role" value="Patient" />
                                            <button type="submit" class="btn btn-sm btn-outline-danger" title="Delete">
                                                <i class="fas fa-trash-alt"></i> Delete
                                            </button>
                                        </form>
                                    </div>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            }
            else
            {
                <div class="alert alert-info">No patients found</div>
            }
        </div>
    </div>
</div>

@section Scripts {
    <script>
        // Activate Bootstrap tabs
        $(function () {
            $('#userTabs a').on('click', function (e) {
                e.preventDefault();
                $(this).tab('show');
            });

            // Preserve tab selection on page reload
            if (location.hash) {
                $('a[href="' + location.hash + '"]').tab('show');
            }

            // Update hash when tab changes
            $('a[data-toggle="tab"]').on('shown.bs.tab', function (e) {
                history.pushState(null, null, e.target.hash);
            });
        });
    </script>
}